/**
 * A container for raycasting flags.
 */

export class Flags {

	/**
	 * Creates a new container for raycasting flags.
	 */

	constructor() {

		/**
		 * The current flags.
		 *
		 * @type {Number}
		 */

		this.value = 0;

	}

}
